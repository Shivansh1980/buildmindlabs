# Optional public deployment override. VITE_* values are bundled into the
# frontend and must never contain secrets. The JSON content file provides the
# production default when this value is omitted.
# VITE_CONTACT_API_URL=https://ryzenshivansh.pythonanywhere.com/api/v1/contact
